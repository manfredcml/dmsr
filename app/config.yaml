sources:
  - name: "local-postgres"
    source_type: postgres
    host: "localhost"
    port: 5432
    user: postgres
    password: postgres

streamer:
  kind: kafka
  name: test-streamer
  kafka_config:
    bootstrap_servers: localhost:9092
